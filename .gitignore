node_modules/
.env
.env.local
.env.production.local
dist/
.next/
coverage/
playwright-report/
test-results/
*.xlsx.tmp
testing_excel.xlsx
.DS_Store
*.tsbuildinfo
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal
backend/logs/

# ── Sensitive / credential files — never commit these ──────
# Session cookie dumps
cookies.txt
cookies_frontend.txt
frontend/cookies.txt

# Env backup files (may contain real credentials)
*.backup
**/*.backup

# Firebase / GCP service account keys
fcm-service.json
**/fcm-service.json
*-service-account*.json
*-credentials*.json

# Temporary archives / IDE artifacts
vfs_backup.zip
**/*.zip

# Root-level .env for docker-compose variable substitution
# (holds POSTGRES_PASSWORD and other compose-level vars)
.env.compose
